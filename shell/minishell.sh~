#!/bin/bash

#check the command

goodCommand="false"

if [ -z $1 ]
then
    echo "error no command"
else

    if [ $1 = "touch" ]
       
    then

    fi
    

    if [ $1 = "copy" ]
     
    then

    fi

    if [ $goodCommand = "false" ]
    then
	echo "wrong command"
    fi
    
fi
